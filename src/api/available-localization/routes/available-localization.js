'use strict';

/**
 * available-localization router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::available-localization.available-localization');
