'use strict';

/**
 * available-localization service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::available-localization.available-localization');
